# action consts
LAST_ACTION = 'last_action'
LAST_DATA = 'last_data'
NOTE_ACTION, CHANGE_ACTION, CREATE_GROUP_ACTION, EMPTY_ACTION = range(4)

# callback const
MESSAGE_CALLBACK, MESSAGE_ACTION_CALLBACK = map(str, range(2))
DONE, CHANGE = map(str, range(2))
